// wasm-js generates this module in `dist`.  We refer to it with `../dist` so that
// the path works from the source in `src/` and from the compiled output in `dist/`.
export * from '../dist/wasm_js_example.js';
